
/**
 * @file demo组件
 * @param {*} props 
 */

import './index.less';

export default function Demo (props) {
    let { } = props;
    return (
        <div class="demo">
            ...
        </div>
    )
}